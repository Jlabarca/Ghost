app:
  id: "ghost-father-daemon"  # Override base
redis:
  enabled: true               # Override base
  database: 1                 # Override base
    # Inherits all other Redis settings from base

    # cli.ghost.yaml (150 lines - only overrides)
  # CLI-specific feature
  # Inherits ALL other settings from base
